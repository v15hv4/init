# cli tools
alias ls="eza"
alias vim="nvim"
alias cat="bat --theme gruvbox-light"
alias df="duf"

# short commands
alias v="vagrant"
alias dc="docker-compose"
alias xo="xdg-open"
alias rs="rsync -avzWP"
alias ap="ansible-playbook"

# containerized texlive
texlive() {
  docker run --rm -it -v $(pwd):/home danteev/texlive:TL2017 $@
}
